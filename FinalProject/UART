int main(void) {
    adc_init();
    uart_init();

    while (1) {
        int reading = analog(0); // ADC0
        if (reading > 900) {
            write_uart("5V Present\r\n");
        } else {
            write_uart("No Voltage\r\n");
        }
        _delay_ms(1000);
    }
}
